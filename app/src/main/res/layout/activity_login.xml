<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.numiracles.firetv.tvlogin.LoginActivity"
    android:onClick="LoginWithAmazon">


    <Button
        android:text="Register with Email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="225dp"
        android:id="@+id/ButtonEmailLogin"
        android:onClick="LoginWithEmail" />


    <ImageButton android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ButtonEmailLogin"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:id="@+id/buttonLWA"
        android:src="@color/login_button_image_state"
        android:background="@android:color/transparent"
        android:padding="0dp"
        android:contentDescription="@string/login_button_content_description"
        android:onClick="LoginWithAmazon" />

    <TextView
        android:text="This is a sample app which demonstrates the different login methods for Fire TV and tells you how many clicks customers will have to make to register with your TV app. It is designed to help developers make great UX decisions and to avoid churn when registering customers."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="65dp"
        android:id="@+id/textView"
        android:textSize="24sp" />

</RelativeLayout>
